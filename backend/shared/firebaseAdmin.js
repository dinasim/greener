// DEPRECATED: firebase-admin no longer used (Expo push only). Safe to delete this file.
// (Left in place to avoid import errors if any stale requires remain.)
module.exports = { initFirebase: () => null };
