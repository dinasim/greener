import PlaceholderMap from './PlaceholderMap';

// Export the components with the same API as react-native-maps
export const Marker = PlaceholderMap.Marker;
export const Callout = () => null;

// For default export (MapView)
export default PlaceholderMap;